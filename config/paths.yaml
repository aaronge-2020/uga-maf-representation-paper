# Default local path template.
# Relative paths are resolved from the bundle root.

raw_data:
  mc3_source_dir: ${datasets_root}/datasets/tcga_mc3
  hrd_assets_dir: ${datasets_root}/datasets/tcga_brca_hrd
  kucab_raw_dir: ${datasets_root}/datasets/kucab_mendeley/raw_mutation_tables
  pcawg_dir: ${datasets_root}/datasets/pcawg_pancan/cgr_validation_results/research/data/pancan_pcawg_2020
  grch37_dir: ${datasets_root}/references/grch37

processed_helpers:
  mc3_standard_features_dir: null

workspace:
  work_dir: ${repro_root}/w
  results_tables_dir: results/tables
  results_figures_dir: results/figures
  results_logs_dir: results/logs
